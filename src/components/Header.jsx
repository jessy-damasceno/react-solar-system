import { Component } from 'react';

const React = require('react');

export default class Header extends Component {
  render() {
    return (
      <header>
        <h1>
          Sistema Solar
        </h1>
      </header>
    );
  }
}
